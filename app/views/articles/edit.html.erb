<% provide(:page_title, "Edit #{@article.title}") %>

<h1 class="text-center"><%=yield(:page_title) %> </h1>

<%= render 'form', article: @article %>

<%= link_to 'Show', @article %> |
<%= link_to 'Back', articles_path %>
