<!-- Popular tags -->
<div class="well">
  <h4>Popular tags</h4>
    <div id="tag_cloud">
		  <% tag_cloud Article.tag_counts, %w[s m l] do |tag, css_class| %>
		    <%= link_to tag.name, tag_path(tag.name), class: "tag has-counter" %>
		  <% end %>
	  </div>
</div>