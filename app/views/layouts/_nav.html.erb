      <!-- Static navbar -->
      <nav class="navbar navbar-default"  id="wellcome">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <%= link_to "ioable", root_path, class: "navbar-brand"%>
          </div>
          <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
              <li ><%= link_to "About", "/about" %></li>
              <li ><%= link_to "CS prework", "/articles/check-list" %></li>
              <% if user_signed_in? %>
              <!--This block is only visible to signed in users -->
              <% end %>
            </ul>
            <ul class="nav navbar-nav navbar-right">
             <% if user_signed_in? %>
                <li class="active"><%= link_to 'New Article', new_article_path %></li>
              <% end %>
              <li>
                <%= form_tag articles_path, method: :get, class: "form-inline", style: "padding: 10px;" do %>
                  <%= text_field_tag 'term', params[:term], placeholder: "Enter text you want to find", class: "form-control"%>
                  <%= button_tag(type: 'submit', class: "btn btn-default") do %>
                    <span class="glyphicon glyphicon-search"></span>
                  <% end %>
              <% end %>
              </li>
              <% unless user_signed_in? %>
                <li><%= link_to 'Login', new_user_session_path  %></li>
                <li><%= link_to 'Sign Up', new_user_registration_path  %></li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div><!--/.container-fluid -->
      </nav>
